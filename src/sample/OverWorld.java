package sample;

public class OverWorld {
    int playerX, playerY;

    public OverWorld() {
        this.playerX = (int) (Math.random() * 30 + 50);
        this.playerY = (int) (Math.random() * 30 + 50);
    }
}
